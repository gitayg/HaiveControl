# Statically link the MSVC C runtime into Windows binaries so they run on any
# machine without the Visual C++ Redistributable (no VCRUNTIME140.dll needed).
[target.x86_64-pc-windows-msvc]
rustflags = ["-C", "target-feature=+crt-static"]
